# Core — CPU build (Phase 1-5,7,8 run on CPU; move to CUDA wheel for GPU perf phases)
# Install torch from the CPU index:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2,<3.0

# HuggingFace: weights, tokenizer, and the reference implementation we validate against
transformers>=4.51        # Qwen3 architecture support landed in 4.51
tokenizers>=0.20
safetensors>=0.4
huggingface-hub>=0.24
accelerate>=0.30

numpy>=1.24

# Serving (Phase 8)
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27               # async load-test client

# Dev / test
pytest>=8.0
rich>=13.0                # readable benchmark tables in the terminal
